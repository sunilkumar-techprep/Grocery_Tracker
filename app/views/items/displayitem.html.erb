<!DOCTYPE html>
<html>
  <head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
    #fresh-until {
  font-weight: bold;
}


      /* Email styles need to be inline */
    </style>
  </head>

  <body>

<nav class="navbar navbar-dark bg-primary">
    <div class="container">
      
    
    <a  class="navbar-brand" class="navbar-nav" href="/">Home</a>
    <a  class="navbar-brand" class="navbar-nav" href="resume.html">Resume</a>
    <a  class="navbar-brand" class="navbar-nav" href="aboutme.html">AboutMe</a>
    <a  class="navbar-brand" class="navbar-nav" href="techskill.html">Technical skills</a>
    <a  class="navbar-brand" class="navbar-nav" href="contact.html">Contact</a>
    <a  class="navbar-brand" class="navbar-nav" href="mission.html">Mission</a>
    <a  class="navbar-brand" class="navbar-nav" href="portfolio.html"> Portfolio</a>
    
    </div>
    </div>
  </nav>



<div>
  <div>
    <table border="1">
      <tr>
        <th>
          ID
        </th>

        <th>
          Name
        </th>

        <th>
          Purchase date
        </th>

        <th>
          Expiration date
        </th>

        <th>
          Quantity
        </th>

        <th>
          Description
        </th>

        <th>
          Image
        </th>

        <th>
          Location
        </th>

        <th>
          Category
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>Details
        </th>
      </tr>
      
      <tbody>
  
  
  <% @list_of_items.each do |an_item| %>
  <tr>
    <td>
      <%= an_item.id %>
    </td>

    <td>
      <%= an_item.name %>
    </td>

    <td>
      <%= an_item.purchase_date %>
    </td>

    <td>
      <%= an_item.expiration_date %>
    </td>

    <td>
      <%= an_item.quantity %>
    </td>

    <td>
      <%= an_item.description %>
    </td>

    <td>
      <%= an_item.image %>
    </td>

    <td>
      <%= an_item.location %>
    </td>

    <td>
      <%= an_item.category %>
    </td>

    <td>
      <%= time_ago_in_words(an_item.created_at) %> ago
    </td>

    <td>
      <%= time_ago_in_words(an_item.updated_at) %> ago
    </td>

    <td>

   <% @diff =(an_item.expiration_date - an_item.purchase_date).to_i%>
    <% if @diff < 10 %>
     <div id="fresh-until" style="color: red;">
    Fresh until <%= @diff %> days
     </div>
    <% else %>
     <div id="fresh-until" style="color: black;">
    Fresh until <%= @diff %> days
     </div>
    <% end %>

    <!--% @diff = (an_item.expiration_date - an_item.purchase_date).to_i %>
        <span class="<%= @diff < 7 ? 'red' : 'black' %>">
       fresh until <%= @diff %> days
     </span>
      <% @diff =(an_item.expiration_date - an_item.purchase_date).to_i%>
      <% if @diff>7 %>
        <div style="color: black;">
          fresh until <%= @diff %> days
        </div> 
      <% else %>
        <div style="color:red;">
          fresh until <%= @diff %> days
        </div>   
      <% end %>
    </td-->

    <td>
      <a href="/items/<%= an_item.id %>">
        Show details
      </a>
    </td>
  </tr>
<% end %>
